# 样本分组信息对齐

## 分组来源
- 文件：`样本分组信息.xlsx`，字段：`Group` / `Sample` / `样本编号`。
- 分组：23 例样本，`ypN0` vs `ypN+`。

## 动手前必做（校验清单）
1. 分组表里的样本编号 与 数据文件名（矩阵目录）**一一对应**，无遗漏、无重复。
2. 每个样本的 `Group` 标签正确（ypN0 / ypN+），无空值、无错别字。
3. 若有编号对不上、多出或缺失，**先标注出来再继续**，不要静默跳过。

## 注意
- 元信息与数据文件的任何不一致，都要在结果中显式标注，不能悄悄「自动纠正」。
