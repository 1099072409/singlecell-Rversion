# ===========================================================
# R / Seurat 单细胞分析项目 —— 忽略规则
# 策略：按「扩展名 / 二进制类型」忽略，不按目录整体忽略
# 原因见 Git使用规范.md 第 3 节：
#   - exclude/ 虽名含"排除"，实为备选方案脚本目录，内含 6 个必入库的 .R
#   - output/ 内含 25 个 <16 KB 的 csv 结果表，是流程可复现的数值证据
# ===========================================================

# ---------- R / RStudio 会话与用户状态文件 ----------
.Rhistory
.RData
.RDataTmp
.Rapp.history
.Rproj.user/
.Ruserdata
.Renviron          # 常含 API key、本地绝对路径，绝不入库

# ---------- R 序列化对象（本项目主要体积来源，共 13 GB）----------
*.rds
*.RDS
*.rda
*.rdata

# ---------- 图形产物（56 个 / 206 MB，均可由脚本重绘）----------
*.pdf
*.png
*.jpg
*.jpeg
*.tiff
*.tif
*.svg

# ---------- 运行日志 ----------
*.log

# ---------- 系统杂项 ----------
.DS_Store
Thumbs.db
desktop.ini

# ---------- 共享数据目录联接（worktree 中的 data junction → 上级 ../data）----------
data/

# ---------- 显式白名单（兜底，防止上面规则误伤）----------
!*.R
!*.r
!*.md
!*.csv
!*.tsv
!*.txt
!*.Rproj
!.gitignore
!.gitattributes
