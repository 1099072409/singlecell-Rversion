# ===========================================================
# R / Seurat 单细胞分析项目 —— 忽略规则
# 策略：按「扩展名 / 二进制类型」忽略，不按目录整体忽略
# 当前约定（2026-09-01 起）：版本库只保留 .R 脚本 + 本文件 + .gitattributes
#   - csv 结果表与 md 文档仍保留在本地磁盘，但不入库（用 git rm --cached 移出）
#   - 若日后想重新入库某个文件：git add -f <路径>
#   - 历史提交中仍可完整取回：git show <提交>:<路径>
# ===========================================================

# ---------- R / RStudio 会话与用户状态文件 ----------
.Rhistory
.RData
.RDataTmp
.Rapp.history
.Rproj.user/
.Ruserdata
.Renviron          # 常含 API key、本地绝对路径，绝不入库

# ---------- R 序列化对象（本项目主要体积来源，共 13 GB）----------
*.rds
*.RDS
*.rda
*.rdata

# ---------- 图形产物（56 个 / 206 MB，均可由脚本重绘）----------
*.pdf
*.png
*.jpg
*.jpeg
*.tiff
*.tif
*.svg

# ---------- 运行日志 ----------
*.log

# ---------- 结果表与文档（本地保留，不入库；仓库仅保留 .R 脚本）----------
*.csv
*.md

# ---------- 系统杂项 ----------
.DS_Store
Thumbs.db
desktop.ini

# ---------- 共享数据目录联接（worktree 中的 data junction → 上级 ../data）----------
data/

# ---------- 显式白名单（兜底，防止上面规则误伤）----------
!*.R
!*.r
!*.Rproj
!.gitignore
!.gitattributes
# 注：!*.md / !*.csv 已移除 —— 否则会覆盖上面的 *.md / *.csv 忽略规则，
#     导致 git add -A 又把结果表和文档加回仓库。
